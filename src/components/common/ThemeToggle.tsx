'use client';

import React from 'react';

export const ThemeToggle: React.FC<{ className?: string }> = () => {
  // Light Mode Only: No user-facing theme toggle UI
  return null;
};

export default ThemeToggle;
